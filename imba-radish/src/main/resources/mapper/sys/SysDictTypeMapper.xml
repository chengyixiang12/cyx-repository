<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soft.sys.mapper.SysDictTypeMapper">

    <select id="getdictTypes" resultType="DictTypesVO">
        SELECT
        sdt.id,
        sdt.dict_name,
        sdt.dict_type,
        sdt.`status`,
        sdt.sort_order
        FROM
        sys_dict_type sdt
        WHERE
        sdt.del_flag = '0'
        <if test="request.keyword != null and request.keyword != ''">
            AND sdt.dict_name LIKE concat('%', #{request.keyword}, '%')
        </if>
        <if test="request.status != null">
            AND sdt.status = #{request.status}
        </if>
        order by sdt.sort_order desc
    </select>
    <select id="getDictType" parameterType="java.lang.Long" resultType="DictTypeVO">
        SELECT
        sdt.id,
        sdt.dict_name,
        sdt.dict_type,
        sdt.sort_order,
        sdt.status,
        sdt.remark
        FROM
        sys_dict_type sdt
        WHERE
        sdt.del_flag = '0'
        AND sdt.id = #{id}
    </select>
    <update id="enableDictType" parameterType="java.lang.Long">
        UPDATE sys_dict_type
        SET status = 1
        WHERE del_flag = '0'
        AND id = #{id}
    </update>
    <update id="forbiddenDictType" parameterType="java.lang.Long">
        UPDATE sys_dict_type
        SET status = 0
        WHERE del_flag = '0'
          AND id = #{id}
    </update>
</mapper>
